/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.google.com', 'www.shutterstock.com'], // Add the domains you need
    },
}
export default nextConfig;
